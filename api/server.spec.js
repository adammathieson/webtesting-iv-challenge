const request = require('supertest');
const server = require('./server');

describe('server.js', () => {
    describe('get /', () => {
        it('should return status 200', async () => {
            
        })
    }


})